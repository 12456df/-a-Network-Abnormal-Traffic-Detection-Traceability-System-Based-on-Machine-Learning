# PCAP / CICFlowMeter (Scheme A). Python 3.12+ required for cicflowmeter==0.5.0.
# Manual steps (Npcap, tcpdump, Python install): see PCAP_MANUAL_SETUP_zh.txt
# After pip install: run tools/patch_cicflowmeter_sniffer.py once (fixes cicflowmeter 0.5.0 CLI bug).

cicflowmeter==0.5.0
pandas==2.2.3
numpy>=1.26.2,<3
scipy>=1.11.4
scapy>=2.5.0
requests>=2.31.0
